$('#tweets').append("<%= escape_javascript(render :partial => 'tweets/list',
                                          :locals => { }) %>");
<% if @page > 0 %>
  $('#tweets-more').find('input[name="page"]').val("<%= @page %>");
<% else %>
  $('#tweets-more').remove();
<% end %>

